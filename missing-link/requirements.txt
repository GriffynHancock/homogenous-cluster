# Pins refreshed 2026-08-17. The previous set (fastapi 0.115.6, uvicorn 0.34.0,
# jinja2 3.1.5, python-multipart 0.0.20, pytest 8.3.4) dated from Dec 2024/Jan
# 2025 and had never been reviewed -- they came from the original plan, which was
# written against whatever versions its author knew about, not against what was
# current. GitHub Dependabot flagged 9 advisories (3 high) on the default branch.
#
# This is the web-facing component of a machine that will hold sensitive
# documents, so stale pins here are not cosmetic. Re-check them whenever
# Dependabot fires, and re-run `python -m pytest tests/ -v` after any bump.
fastapi==0.141.1
uvicorn[standard]==0.52.3
httpx==0.28.1
jinja2==3.1.6
pytest==9.1.1
python-multipart==0.0.32
# PDF text extraction. Uploads used to be decoded as UTF-8 with errors="replace",
# which turned every PDF into mojibake that was then summarised and stored as a
# successful job. See missing_link/extract.py.
pypdf==6.16.1

# Sentence segmentation -- a LEGAL-DOMAIN Punkt variant (ALEA Institute, MIT).
# Adopted on measurement, not on reputation: F48 ran five candidate splitters
# against the real corpus in the job store and pysbd -- the obvious choice,
# peer-reviewed, 97.92% Golden Rules -- scored WORSE than the regex it would
# have replaced (2.76% vs 2.85% marker density) and took 133.94 s to do it.
# nupunkt cut structural fragments 65.0% -> 12.3% and restored the genre
# separation F45 lost. See missing_link/sentences.py.
#
# It is the only entry in this file that is OPTIONAL: missing_link.sentences
# falls back to a dependency-free regex and says so loudly. Leaving it out
# does not break the pipeline, it invalidates the numbers.
#
# ZERO transitive dependencies, model bundled in the wheel, nothing fetched at
# first use (verified in a network namespace with no interfaces). REQUIRES
# PYTHON >= 3.11; nodes 1 and 2 are on 3.11.2, exactly on the line.
nupunkt==0.6.0
